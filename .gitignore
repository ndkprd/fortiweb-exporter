# Local FortiWeb credentials/config — never commit the real one.
# Anchored to the repo root only, so examples/config.yml (placeholder data,
# meant to be committed) isn't swept up by this rule.
/config.yml

# Build artifacts
/fortiweb-exporter
/bin/

# Go
*.test
*.out

# OS/editor cruft
.DS_Store
*.swp

# Claude Code local session state
.claude/

.codegraph
.agents/
